#include <iostream>

using namespace std;

    int main()
    {
        long int a, b;

        cin >> a >> b;

        long int result = (a + b) * (a - b);

        cout << result << endl;

        return 0;
    }
