#include <iostream>

using namespace std;

    int main()
    {
        long int n, m;
        
        cin >> n >> m;

        if (n == m)
            cout << true << endl;
        else if (n != m)
            cout << false << endl;

        return 0;
    }
