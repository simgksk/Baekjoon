#include <iostream>

using namespace std;

    int main()
    {
        long int n, m;

	cin >> n >> m;

	if (n - m < 0)
		cout << (-2 * (n - m)) + (n - m) << endl;
	else if(n - m >= 0)
		cout << n - m << endl;
        
        return 0;
    }
