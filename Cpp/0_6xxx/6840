#include <iostream>

using namespace std;

    int main()
    {
#pragma region 6840번. Who is in the middle?

        int a, b, c;

        cin >> a;
        cin >> b;
        cin >> c;

        if ((a > b && a < c) || (a<b && a>c))
            cout << a << endl;
        else if ((b > a && b < c) || (b<a && b>c))
            cout << b << endl;
        else if ((c > a && c < b) || (c<a && c>b))
            cout << c << endl;

#pragma endregion

        return 0;
    }

